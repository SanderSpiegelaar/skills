# Agent skills block

Use this block in `AGENTS.md` or `CLAUDE.md` after running setup.

```markdown
## Agent skills

### Issue tracker

[one-line summary of where issues are tracked]. See `docs/agents/issue-tracker.md`.

### Triage labels

[one-line summary of the label vocabulary]. See `docs/agents/triage-labels.md`.

### Domain docs

[one-line summary of layout — "single-context" or "multi-context"]. See `docs/agents/domain.md`.
```

Keep the summaries short. Do not duplicate the full contents of `docs/agents/*.md` in the agent instruction file.
